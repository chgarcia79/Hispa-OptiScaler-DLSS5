---
tags: [diario]
fecha: '2026-09-12'
---

# Diario - 2026-09-12

## 🎯 Tareas Completadas
- Estandarización y reorganización integral del versionado SemVer de los paquetes de **Hispa-OptiScaler DLSS 5**:
  - Renombrado de la versión inicial a `v1.0.0-dlss5` (anteriormente `v10.0.0-dlss5`).
  - Creación y configuración de la versión predeterminada `v1.0.1-dlss5` (basada en el upstream estable OptiScaler v0.9.4 con integración completa de DLSS 5 Neural Rendering).
  - Limpieza y sincronización de binarios, archivos `.ini` y empaquetados `.zip` en `Src/DESKTOP/HispaGameControl/Repository/OptiScaler/`, en el repositorio de bibliotecas `K:\Librerias\HispaGameControl\Repository\OptiScaler/` y de forma instantánea a través del Junction NTFS en `C:\Soft\Juegos\HispaGameControl\Repository\OptiScaler/`.
- Implementación de normalizador transparente de retrocompatibilidad en C#:
  - Inyección del método `GameInfo.NormalizeOptiScalerVersionString(string? version)` en `GameInfo.cs` para mapear automáticamente identificadores históricos (`v10.0.0-dlss5` -> `v1.0.0-dlss5`, `v0.9.4-dlss5` -> `v1.0.1-dlss5`).
  - Actualización de getters y setters de `SelectedOptiScalerProfileVersion` y `OptiScalerVersion` para auto-normalizar cualquier lectura de base de datos SQLite o configuración guardada.
  - Actualización de `DlssSwapperService.cs`, `MainViewModel.cs` y `OptiScalerModule.cs` para establecer `v1.0.1-dlss5` como la versión por defecto de inyección en nuevos juegos.
  - Actualización de `ExtraerLibrerias.ps1` para el desempaquetado de ambas versiones oficiales.
- Verificación y despliegue:
  - Compilación 100% limpia (0 errores) con `dotnet build` en Release.
  - Publicación a producción en `C:\Soft\Juegos\HispaGameControl\` según regla `[PUBLICACION_LOCAL_DESKTOP]`.
  - Creación y push de las etiquetas de versión oficiales `v1.0.0-dlss5` y `v1.0.1-dlss5` en el repositorio remoto `chgarcia79/Hispa-OptiScaler-DLSS5`.
- Actualización de la base de conocimiento Zettelkasten:
  - Actualización de [[arq_motor_hispaoptiscaler_dlss5]] reflejando la nueva nomenclatura SemVer y el mecanismo de normalización transparente.
- Optimización de UI y alineación en `GameDetailsDialog.xaml`:
  - Reestructuración del panel de la Suite de Renderizado Neural (DLSS 5) en dos filas horizontales dedicadas:
    - Fila 1: `Método de Inyección` y a su derecha `Versión OptiScaler` (o `Perfil RenoDX`).
    - Fila 2: `Versión Neural` y badge informativo de `ReShade para Color activo`.
  - Eliminación de saltos de línea erráticos del antiguo `WrapPanel`, asegurando alineación directa y balance estético con los botones de acción.
  - Validación de compilación Release (0 errores) y publicación en producción en `C:\Soft\Juegos\HispaGameControl\`.
- Corrección y estandarización del ordenamiento de versiones (SemVer / Numérico descendente):
  - Implementación del comparador universal `DlssSwapperService.CompareVersionStrings(string? a, string? b)` para ordenar de forma numérica y semántica los componentes de versión (ej: `v310.9.1` > `v310.9.0` > `v310.7.129` > `v3.7.10`).
  - Aplicación de orden descendente en todas las colecciones de librerías (`GetVersionsForType`, `GetDownloadedVersionsForCategory`, `GetAvailableDlssNrVersions`, `GetAvailableOptiScalerVersions`).
  - Corrección en `MainViewModel.cs`: sustitución de `Insert(0)` forzado por inserción y re-ordenamiento descendente integral, garantizando que siempre aparezcan primero las versiones más nuevas.
  - Publicación y despliegue actualizado en producción en `C:\Soft\Juegos\HispaGameControl\`.
- Creación de la arquitectura para el Bucle Rápido de Desarrollo (Inner Loop):
  - Creación del script atómico transversal [Invoke-FastDevLoop.ps1](file:///T:/DESARROLLO/GCLOUD/UTILIDALES/SCRIPTS/Invoke-FastDevLoop.ps1) en `T:\DESARROLLO\GCLOUD\UTILIDALES\SCRIPTS\`: cierre de procesos bloqueantes en memoria, compilación/publicación incremental sin restore en menos de 0.5s y relanzamiento de aplicación.
  - Actualización del inventario de utilidades en [README.md](file:///T:/DESARROLLO/GCLOUD/UTILIDALES/SCRIPTS/README.md).
  - Creación de la Skill global [fast-dev-loop](file:///C:/Users/Chgar/.gemini/config/skills/fast-dev-loop/SKILL.md) en `C:\Users\Chgar\.gemini\config\skills\fast-dev-loop\`: protocolo de un solo viaje (single-trip) para iteraciones ultrarrápidas de UI/lógica sin burocracia intermedia, coordinada con `git-workflow` para el cierre final.
- Optimización de valores por defecto en Suite Neural y soporte DLSS-NR v310.9.1:
  - Implementación de preselección inteligente condicional en `MainViewModel.cs`: cuando un juego no tiene el pack inyectado (`!game.IsStreamlineNeuralActive`), se preselecciona automáticamente la versión más reciente de OptiScaler (`v1.0.1-dlss5`) y la versión más reciente de Neural (`v310.9.1 (Oficial)`).
  - Preservación fiel de la versión inyectada activa (`game.OptiScalerVersion`, `game.ActiveDlssNrVersion`) cuando el pack ya se encuentra instalado.
  - Incorporación de `v310.9.1 (Oficial)` a las versiones disponibles de DLSS-NR en `DlssSwapperService.cs` y resolución con fallback seguro en `ResolveDlssNrDirectory`.
  - Actualización de los valores por defecto en `GameInfo.cs` a `v310.9.1 (Oficial)`.
  - Compilación Release limpia (0 errores) y publicación en producción en `C:\Soft\Juegos\HispaGameControl\`.
- Corrección de crash (DEP / Use-After-Free) de OptiScaler v1.0.1-dlss5 en Cyberpunk 2077 + ReShade:
  - Diagnóstico: En `wrapped_swapchain.cpp`, el constructor de `WrappedIDXGISwapChain4` liberaba inmediatamente `_real3` tras `QueryInterface`, dejando un puntero colgante que provocaba un fallo de segmentación/DEP (`0xC0000005` at `0x0`) al llamar a `IDXGISwapChain3::SetColorSpace1(0)` tras `ResizeBuffers`.
  - Cirugía C++20: Preservación de interfaces COM `_real1`–`_real4` vivas en el constructor, liberación limpia en `~WrappedIDXGISwapChain4()`, y adición de *null-safety* con re-query fallback en `SetColorSpace1`, `CheckColorSpaceSupport`, `GetCurrentBackBufferIndex` y `SetHDRMetaData`.
  - Commit `fa122108` en rama `feature/upstream-0.9.4-dlss5` y push al remoto `hispa-optiscaler` (`git@github.com-personal:chgarcia79/Hispa-OptiScaler-DLSS5.git`).
  - Compilación remota automatizada en GitHub Actions (Workflow `Build Hispa-OptiScaler-DLSS5`, Run 34657770952, finalizado con éxito en 5.92 min).
  - Descarga de la release `nightly` y despliegue de los binarios parcheados (`OptiScaler.dll`, `nvngx.dll_dlssnr.dll`) en:
    - `Src/DESKTOP/HispaGameControl/Repository/OptiScaler/v1.0.1-dlss5/`
    - `C:\Soft\Juegos\HispaGameControl\Repository\OptiScaler/v1.0.1-dlss5/`
    - `K:\Librerias\HispaGameControl\Repository\OptiScaler/v1.0.1-dlss5/`
    - `J:\Steam\steamapps\common\Cyberpunk 2077\bin\x64\dxgi.dll`
- Gobernanza de compilación de motor C++ en `.agent/rules.md`:
  - Inyección de la regla determinista `[COMPILACION_MOTOR_OPTISCALER]` según skill `gestor-reglas` con backup inmutable en `.agent/rules_BAK_20260912_012547.md`.
  - Prohibición estricta de compilación local con MSBuild/VS y obligación de delegación en GitHub Actions.
- Diagnóstico del Minidump de REDEngine y Resolución de Crash en SetColorSpace1:
  - Análisis de bajo nivel del crash dump `Cyberpunk2077.dmp` (`TID 6896`): identificado fallo de violación DEP en `0x0` provocado por puntero `this` (`RCX`) nulo (`0x0000000000000000`).
  - Causa raíz identificada: `WrappedIDXGISwapChain4::QueryInterface` rechazaba indebidamente las interfaces `IDXGISwapChain1` hasta `IDXGISwapChain4` devolviendo `E_NOINTERFACE` y `*ppvObject = nullptr` cuando los punteros subyacentes `_real1..4` aún no estaban resueltos o existía intermediación de Streamline (`sl.interposer.dll`) / ReShade. El juego o ReShade, al asumir la existencia de `IDXGISwapChain3`, invocaba `SetColorSpace1` sobre el puntero nulo devuelto.
  - Corrección en `OptiScaler/wrapped/wrapped_swapchain.cpp`:
    - `QueryInterface` canónico: devuelve siempre `S_OK` con `static_cast<IDXGISwapChainX*>(this)` para todas las interfaces COM implementadas.
    - Resolución perezosa (`lazy query`) de `_real1..4` en cada invocación.
    - Blindaje nulo en `CheckColorSpaceSupport`, `SetColorSpace1`, `ResizeBuffers1`, `GetCurrentBackBufferIndex` y `SetHDRMetaData`.
    - Integración de la directiva `SkipColorSpace` directamente en `SetColorSpace1`.
  - Compilación remota en GitHub Actions: commit `bced3855` en rama `feature/upstream-0.9.4-dlss5` (Run `34660475612`), finalizado con `success`.
  - Descarga del paquete nightly `OptiScaler_v0.9.4-pre0_20260912.zip` y despliegue físico de `dxgi.dll`, `OptiScaler.dll` y `nvngx.dll_dlssnr.dll` en `J:\Steam\steamapps\common\Cyberpunk 2077\bin\x64\`, así como en los repositorios de `v1.0.1-dlss5`.
- Resolución definitiva de Crash (DEP / Access Violation en 0x0) post-ResizeBuffers y slDLSSGGetState en Cyberpunk 2077:
  - Análisis forense de TID 9332: Identificada violación de ejecución DEP (`0xC0000005`) en `0x0000000000000000` (`RCX = 0x0`, `RIP = 0x0`).
  - Causas raíz resueltas:
    1. `WrappedIDXGISwapChain4::QueryInterface` carecía de fallback delegatorio hacia `_real->QueryInterface(riid, ppvObject)` para interfaces no envueltas de DXGI (`IDXGISwapChainMedia`, extensiones internas de driver/runtime), retornando `nullptr` a la invocación COM.
    2. Métodos de `IDXGISwapChain1` e `IDXGISwapChain2` carecían de verificación e inicialización perezosa de `_real1` y `_real2`.
    3. En `StreamlineHooks::hkslDLSSGGetState`, OptiScaler sobreescribía los contadores de frames presentados (`state.numFramesActuallyPresented = 1`) desincronizando las vallas de sincronización de Streamline cuando la RTX 5090 ejecuta DLSS Frame Generation de forma nativa por hardware (`currentFG == nullptr`).
  - Correcciones implementadas en C++:
    - En `OptiScaler/wrapped/wrapped_swapchain.cpp`: fallback en `QueryInterface` a `_real->QueryInterface(riid, ppvObject)` y guardas nulas completas con re-query en todos los métodos de `IDXGISwapChain1` y `IDXGISwapChain2`.
    - En `OptiScaler/hooks/Streamline_Hooks.cpp`: guarda nula en `o_slDLSSGGetState` y bypass estricto de modificación de estado si `currentFG == nullptr`.
    - En `J:\Steam\steamapps\common\Cyberpunk 2077\bin\x64\OptiScaler.ini`: configurado `FGInput=auto` y `FGOutput=auto`.
  - Compilación remota en GitHub Actions:
    - Commit `7ab73320` en rama `feature/upstream-0.9.4-dlss5`.
    - Workflow `Build Hispa-OptiScaler-DLSS5` (Run ID: `34673519424`) completado al 100% con cero errores.
  - Despliegue en producción:
    - Descarga del artefacto `OptiScaler_v0.9.4-pre0_20260912.zip` desde la release `nightly`.
    - Despliegue físico sincronizado de `OptiScaler.dll` y `dxgi.dll` (`25425408` bytes) y `nvngx.dll_dlssnr.dll` en `J:\Steam\steamapps\common\Cyberpunk 2077\bin\x64\`.
    - Limpieza de logs residuales para inicialización limpia del motor.
